{
  "private": true,
  "devDependencies": {
    "turbo": "^1.0.23"
  },
  "turbo": {
    "npmClient": "pnpm",
    "pipeline": {
      "build": {
        "dependsOn": [
          "^build"
        ],
        "outputs": [
          "dist/**",
          ".umi/**",
          ".next/**"
        ]
      },
      "dev": {
        "cache": false
      }
    }
  }
}
